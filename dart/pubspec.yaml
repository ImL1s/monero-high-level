name: monero_dart
description: Pure Dart implementation of Monero wallet functionality.
version: 0.1.0
homepage: https://github.com/ImL1s/monero-high-level
repository: https://github.com/ImL1s/monero-high-level

environment:
  sdk: ^3.0.0

dependencies:
  # Cryptography
  pointycastle: ^3.9.1
  
  # Networking
  http: ^1.2.2
  dio: ^5.7.0
  
  # Serialization
  json_annotation: ^4.9.0
  
  # Utilities
  collection: ^1.18.0
  meta: ^1.15.0
  logging: ^1.3.0

dev_dependencies:
  test: ^1.25.8
  build_runner: ^2.4.13
  json_serializable: ^6.8.0
  lints: ^4.0.0
  mockito: ^5.4.4
  build_verify: ^3.1.0

# Dart 3.10+ features
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
